---
title: "Session 1: Introduction to LLMs as Tools for Linguistic Data Analysis"
subtitle: "Day 1 · Introduction & First Experience (1-1)"
kicker: "Linguistic Data Analysis II · Day 1"
format:
  revealjs:
    # Per-deck flavor. slides-v2 = shared rules; slide-<flavor> = palette only.
    theme:
      - default
      - ../assets/css/slides-v2.scss
      - ../assets/css/slide-forest.scss
metadata-files:
  - _slides.yml
---

<!-- OUTLINE DECK — slide-by-slide beat list for review. Bodies are condensed bullets, not
     final prose; once the structure is approved these get written out in the S2/S3 style
     (framing paragraph + `. . .` fragments + callouts). Derived from course-design.md S1
     (the 7-beat single argument, "Five-day arc" block). -->

# Session 1: Introduction to LLMs as Tools

## 📋 Agenda

0. Self-introductions
1. Why this course
2. The 5-day map & objectives
3. What do you know about LLM?
4. What an LLM is — and why it's relevant
5. LLM follows instructions — a no-code demo
6. But — we need evaluation
7. Discussion


 
# 0 · Self-introductions

## Introduce yourself - Round 1

Tell us:

- your **name** 
- What you enjoy doing while you are not doing research

## Round 2

Tell us:
- What is your research interest

## Round 3

- Why you decided to take this course
- Any goals
- Any concerns

# 1 · Why this course

## LLMs are everywhere now

Large language models are now **common tools** — for writing, translating, coding, searching.

So the question isn't *whether* linguists will use them. It's **how to use them responsibly** for linguistic analysis.

## Linguistic annotation: promising but costly

One especially promising use is **annotation** — labelling language data for analysis.

- But careful annotation is **expensive**: expert time, training, guidelines, double-coding.
- If an LLM could annotate reliably, then it facilitates research.

The tempting question: *can an LLM just do it for us?*

## Adequacy must be evaluated, not assumed

Here's the seed of the whole course:

> An LLM's output is only useful if it's **adequate** — and adequacy has to be **evaluated, not assumed.**


**By Friday you will have *critically evaluated* an LLM on your own annotation task**


## One simple Goal of the course

Can we trust an LLM on identifying collocation?

**Until today**

- Well, maybe? 

OR 

- Are there any paper on this?

**After this course**

- Let's find out! 


Each day earns one piece of the method. By Friday you own the whole loop.

# 2 · The 5-day map

## What we'll do this week {.smaller}

| Day | Focus |
|-----|-------|
| **1** | What is LLM, Python basics, call an LLM |
| **2** | Gold standard + agreement — **the yardstick** |
| **3** | Prompt + iterate against metrics |
| **4** | Method + assemble the pipeline |
| **5** | Your own data + present |

## 🎯 Learning Objectives

By the end of this session, you will be able to:

- Describe, at a high level, **what a Large Language Model is**.
- Say what **machine learning** is and how ML models are built.
- Explain, at a high level, **how LLMs are trained** and how they produce natural language.

## The one idea for today {.smaller}

> An LLM is a **judge you must check**, not an oracle — and it fits the linguistic tasks you already care about.


Everything this week is a way of *checking* that judge. Today we build the case for **why** checking is the whole point.

# 3 · What do you know about LLM?

- What is a Large Language Model (or LLM)?

  Explain in your own words.


# 4 · What is an LLM — and why it's relevant

## The plan — follow one example {.smaller}

An LLM is a kind of **statistical machine-learning model**. 

To see its significance, we need to take a detour:

1. **Machine learning** — what "learning from data" means
2. **NLP** — machine learning applied to *language*
3. How NLP researchers try to **encode** meaning 
4. **Transformers** architecture and its application to LLM


## Statistical machine-learning model

**Statistical Machine learning** = a model **learns patterns from data** instead of being hand-coded with rules.

- Rule-based ML: *a human writes the rules.*
- Statistical ML: *the model infers the rules from examples.*

An ML model learns a **probabilistic representation** from statistical regularity in the data — *which patterns tend to go with which outcome.*

## Example — spam detection {.smaller}

The classic ML task: given an email, decide **spam / not-spam**.

- **Rule-based:** block anything containing `FREE`, `WINNER`, `$$$`
  → brittle; spammers just write "F R E E", and you block real mail.

- **ML:** show the model thousands of emails already **labelled** spam / not-spam.
  It *learns on its own* which words (and combinations) predict spam.

The rules are **inferred from examples**, not written by hand.

## What is the predictor in the ML model? 

The idea of ML is like creating a smart regression model.

But then we need **predictors** (input features) to make predictons.

Three different classes of technologies.

- One-hot text vector
- Static Embeddings
- Contextual Embeddings

## One-hot text vector {.smaller}

Each vocabulary word is a **slot**. A word flips on its own slot — **1 if present, 0 if not**. Add the words up → the email's **bag-of-words vector**, and *that* is what the classifier reads.

```{=html}
<style>
.spam-pipe{display:flex;align-items:stretch;justify-content:center;gap:.5rem;flex-wrap:wrap;font-size:.6rem;margin-top:.4rem}
.spam-pipe .pnode{display:flex;flex-direction:column;gap:.3rem;align-items:center;justify-content:center;
  background:var(--surface);border:1.5px solid var(--primary-70);border-radius:.5rem;padding:.5rem .6rem}
.spam-pipe .cap{font-size:.72em;letter-spacing:.06em;text-transform:uppercase;color:var(--ink-soft);font-weight:600}
.spam-pipe .subcap{font-size:.68em;color:var(--ink-soft);font-style:italic}
.spam-pipe .email-txt{font-family:ui-monospace,Menlo,monospace;line-height:1.3;color:var(--ink)}
.spam-pipe .parrow{display:flex;align-items:center;font-size:1.4em;font-weight:700;color:var(--primary)}
.oh-grid{display:grid;grid-template-columns:46px repeat(6,20px);gap:3px;align-items:center}
.oh-grid .rl{font-family:ui-monospace,Menlo,monospace;font-size:.9em;text-align:right;color:var(--ink)}
.oh-grid .hl{writing-mode:vertical-rl;transform:rotate(180deg);font-size:.82em;color:var(--ink-soft);justify-self:center;height:44px}
.oh-grid .c{width:20px;height:20px;display:flex;align-items:center;justify-content:center;border-radius:3px;
  border:1px solid var(--primary-80);font-size:.9em;color:var(--ink-soft)}
.oh-grid .c.on{background:var(--accent);border-color:var(--accent);color:var(--white);font-weight:700}
.vec{display:flex;gap:3px}
.vec .c{width:22px;height:22px;display:flex;align-items:center;justify-content:center;border-radius:3px;
  border:1px solid var(--primary-80);font-size:.9em;color:var(--ink-soft)}
.vec .c.on{background:var(--accent);border-color:var(--accent);color:var(--white);font-weight:700}
.fcell{width:26px;height:22px;display:flex;align-items:center;justify-content:center;border-radius:3px;
  border:1px solid var(--primary-80);font-size:.82em;color:var(--ink)}
.gauge{background:var(--warn);color:var(--white);border-radius:1rem;padding:.15rem .55rem;font-weight:700;white-space:nowrap}
.verdict{font-weight:700;color:var(--warn)}
</style>

<div class="spam-pipe">
  <div class="pnode">
    <div class="cap">Email</div>
    <div class="email-txt">claim your<br>free prize</div>
  </div>
  <div class="parrow">→</div>
  <div class="pnode">
    <div class="cap">One-hot / word</div>
    <div class="oh-grid">
      <div></div><div class="hl">claim</div><div class="hl">your</div><div class="hl">free</div><div class="hl">prize</div><div class="hl">meet</div><div class="hl">notes</div>
      <div class="rl">claim</div><div class="c on">1</div><div class="c">0</div><div class="c">0</div><div class="c">0</div><div class="c">0</div><div class="c">0</div>
      <div class="rl">your</div><div class="c">0</div><div class="c on">1</div><div class="c">0</div><div class="c">0</div><div class="c">0</div><div class="c">0</div>
      <div class="rl">free</div><div class="c">0</div><div class="c">0</div><div class="c on">1</div><div class="c">0</div><div class="c">0</div><div class="c">0</div>
      <div class="rl">prize</div><div class="c">0</div><div class="c">0</div><div class="c">0</div><div class="c on">1</div><div class="c">0</div><div class="c">0</div>
    </div>
  </div>
  <div class="parrow">Σ</div>
  <div class="pnode">
    <div class="cap">Bag-of-words</div>
    <div class="vec"><div class="c on">1</div><div class="c on">1</div><div class="c on">1</div><div class="c on">1</div><div class="c">0</div><div class="c">0</div></div>
    <div class="subcap">sparse · mostly 0</div>
  </div>
  <div class="parrow">→</div>
  <div class="pnode">
    <div class="cap">Classifier</div>
    <div class="gauge">P(spam)=0.94</div>
    <div class="verdict">🚫 spam</div>
  </div>
</div>
```

::: {.fragment .smaller}
**Cost:** the vector is as long as the whole vocabulary (tens of thousands of slots), and *"free"* and *"complimentary"* are just two unrelated slots — no notion that they *mean* the same.
:::

## Static embeddings {.smaller}

An **embedding** is a *learned* **dense** vector for each word — trained so that words with **similar meaning sit close together** (Latent Semantic Analysis, Word2Vec). 

Look up each word, **average** them → the email's vector.

```{=html}
<div class="spam-pipe">
  <div class="pnode">
    <div class="cap">Email</div>
    <div class="email-txt">claim your<br>free prize</div>
  </div>
  <div class="parrow">→</div>
  <div class="pnode">
    <div class="cap">Embedding lookup</div>
    <div class="oh-grid" style="grid-template-columns:46px repeat(4,26px)">
      <div class="rl">claim</div><div class="fcell" style="background:color-mix(in srgb,var(--accent) 62%,transparent)">.8</div><div class="fcell">.1</div><div class="fcell" style="background:color-mix(in srgb,var(--primary) 32%,transparent)">-.3</div><div class="fcell" style="background:color-mix(in srgb,var(--accent) 44%,transparent)">.6</div>
      <div class="rl">your</div><div class="fcell">.0</div><div class="fcell" style="background:color-mix(in srgb,var(--accent) 18%,transparent)">.2</div><div class="fcell">.1</div><div class="fcell">-.1</div>
      <div class="rl">free</div><div class="fcell" style="background:color-mix(in srgb,var(--accent) 68%,transparent)">.9</div><div class="fcell" style="background:color-mix(in srgb,var(--primary) 24%,transparent)">-.2</div><div class="fcell" style="background:color-mix(in srgb,var(--primary) 42%,transparent)">-.4</div><div class="fcell" style="background:color-mix(in srgb,var(--accent) 52%,transparent)">.7</div>
      <div class="rl">prize</div><div class="fcell" style="background:color-mix(in srgb,var(--accent) 52%,transparent)">.7</div><div class="fcell">.0</div><div class="fcell" style="background:color-mix(in srgb,var(--primary) 24%,transparent)">-.2</div><div class="fcell" style="background:color-mix(in srgb,var(--accent) 40%,transparent)">.5</div>
    </div>
    <div class="subcap">4 dims shown (really ~300)</div>
  </div>
  <div class="parrow">⌀</div>
  <div class="pnode">
    <div class="cap">Avg → doc vector</div>
    <div class="vec"><div class="fcell" style="background:color-mix(in srgb,var(--accent) 48%,transparent)">.6</div><div class="fcell">.0</div><div class="fcell" style="background:color-mix(in srgb,var(--primary) 28%,transparent)">-.2</div><div class="fcell" style="background:color-mix(in srgb,var(--accent) 36%,transparent)">.4</div></div>
    <div class="subcap">dense · short</div>
  </div>
  <div class="parrow">→</div>
  <div class="pnode">
    <div class="cap">Classifier</div>
    <div class="gauge">P(spam)=0.91</div>
    <div class="verdict">🚫 spam</div>
  </div>
</div>
```

::: {.fragment .smaller}
**Win:** *"free"* and *"complimentary"* now have **nearby** vectors → the model generalises to words it never saw labelled. **Still** ignores word order (it's an average). → next, **context**.
:::

## The limit of Static embeddings

Meaning of a word depends on order and context:

- "your account is **free** of charge" ✅ not spam
- "claim your **FREE** prize now" 🚫 spam

Same word, opposite signal. This is because:

- Many words are polysemous or homographic
- Isolated word does not adds up to the meaning of the whole
  - idioms such as *kick the bucket*


## Transformer architecture

- **Attention:** every word decides *which other words are relevant to it*
- Output: **context-sensitive** representations of each word and a document

. . .

Take the two *free*s from the last slide:

| Sentence | *free* attends most to | Resulting sense |
|---|---|---|
| "your account is **free** of charge" | *account*, *charge* | banking, benign |
| "claim your **FREE** prize now" | *claim*, *prize*, *now* | promotional |

. . .

Same string, **two different vectors** — because the neighbours differ. Static embeddings could not do this; there was only ever one vector for *free*.

## Why this matters for you {.smaller}

This is the whole reason an LLM can annotate at all.

. . .

The categories you care about — *is this stance hedged?*, *is this move a Gap or a Purpose?*, *what CEFR level is this?* — are **not** properties of the words in isolation. They are properties of words **in context**.

. . .

A bag-of-words model cannot represent that. A transformer can.

. . .

::: {.callout-warning}
"Can represent it" is not "gets it right." That gap is what the rest of the week measures.
:::

## How LLMs are trained {.smaller}

An LLM is trained by **self-supervised pretraining** on huge amounts of text:

- The task: **predict the next word**, over and over, on billions of sentences.
- No hand-labelling needed — the text is its own answer key.

Producing natural language is just **that same next-word prediction, run forward** — which is why it *sounds* fluent.

## Autoregressive model = the LLM {.smaller}

Predicts the token at time *T* given everything before it (*T−1, T−2, …*).

$ P(w_T \mid w_1, w_2, \dots, w_{T-1}) $


Run that forward, one token at a time → fluent, coherent text.
That is exactly what a chat LLM does when it answers you.


## The LLM *re-absorbs* the classic tasks {.smaller}

Historically, **each task had its own model** — POS tagger, NER model, parser, coreference model.

With scale + pretraining + **prompting**, **one model can do many tasks**.


## Summary {.smaller}

An **LLM** is:

- a **statistical machine-learning model** — it learns patterns from data,
- of the **autoregressive** kind — it predicts the next token given the previous sequence,
- built on the **transformer** architecture — attention lets it read context,
- trained by **next-word prediction** on huge amounts of text.


# 5 · LLM follows language instructions

## Throw language data at an LLM {.smaller}

Let's just **try it** — no code:

- paste some real language data into a chat LLM,
- ask it to label / analyze it,
- read the output together.

**What did it get right? What looks off?** And the question that runs the whole course:
*how would we ever **know** — beyond eyeballing?*


# 6 · But — we need evaluation

## The inversion {.smaller}

Classic NLP **consumed** annotation to *build* models.

Now the model can **produce** annotation. The arrow flips:

**before:** humans annotate → train the model
**now:** the model annotates → *and who checks it?*

## Trained to *sound* right, not *be* right {.smaller}

An LLM is optimized to produce **fluent, plausible** text.


Plausible is **not** the same as **correct**. So we check its labels against a **human gold standard**, using NLP's own metrics:

- **inter-annotator agreement** (percent + Cohen's κ)
- **precision / recall / F1**, **confusion matrix**

We'll cover these on Day 2.

## The punchline {.smaller}

::: {.callout-important}
This course is the **evaluation half of NLP, applied to LLMs.**
:::

NLP's *training* half we mostly leave aside. Its **evaluation** half is exactly the rigor an LLM's outputs need — and it's what you'll spend the week learning to run.


# 7 · Discussion 

## Your turn {.smaller}

- What was new information to you about LLM?
- What do you want to know more about? 


# Recap · The 5-day map

## What we'll do this week {.smaller}

| Day | Focus |
|-----|-------|
| **1** | What is LLM, Python basics, call an LLM |
| **2** | Gold standard + agreement — **the yardstick** |
| **3** | Prompt + iterate against metrics |
| **4** | Method + assemble the pipeline |
| **5** | Your own data + present |

# Any questions?
